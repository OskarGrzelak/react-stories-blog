export type ExcerptType = {
  _id: string;
  title: string;
  author: string;
  excerpt: string;
};

export type StoryType = {
  _id: string;
  title: string;
  author: string;
  excerpt: string;
  text: string;
};
